# =============================================================================
# Vercel → em-solar project → Settings → Environment Variables
# Apply to Production (and Preview if you test PR deployments). Then redeploy.
# =============================================================================

VITE_API_URL=https://em-solar-backend-production.up.railway.app
