# em-solar-api (Cloudflare Worker). No trailing slash.
# Local Node API (`npm run dev` in em-solar-backend):
# VITE_API_URL=http://localhost:8787
#
# Live / production API (default in built app if this line is omitted):
VITE_API_URL=https://em-solar-backend.ghaffaryasir28.workers.dev
