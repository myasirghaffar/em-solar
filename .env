# Local UI → local API (`npm run dev` in em-solar-backend). No trailing slash.
VITE_API_URL=http://localhost:8787

# To hit the deployed Worker from a local Vite build instead, comment the line above and use:
# VITE_API_URL=https://em-solar-backend.ghaffaryasir28.workers.dev
