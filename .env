# Default: Vite probes http://localhost:8787/health — if local backend is down, UI uses live API
# (see src/config/api-default-url.ts). To skip probe and force one URL, set VITE_API_URL to full https URL.
VITE_API_URL=http://localhost:8787
# API_BASE_URL=https://em-solar-api.ghaffaryasir28.workers.dev
