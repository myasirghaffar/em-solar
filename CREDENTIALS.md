# Demo login credentials

Use these accounts while the app runs on local dummy data.

## Admin (dashboard)

| Field    | Value                 |
| -------- | --------------------- |
| **Email**    | `admin@energymart.pk` |
| **Password** | `admin123`            |

Opens the **admin dashboard** at `/admin` when you use the profile icon in the header (or after sign-in).

## Customer (storefront profile)

| Field    | Value                     |
| -------- | ------------------------- |
| **Email**    | `ali.khan@example.com`    |
| **Password** | `user123`                 |

Opens **My account** at `/profile` with orders and addresses (matches demo order data).

Other demo customers use the same password **`user123`** with their seed email, for example:

- `sara.malik@example.com`
- `h.raza@example.com`
- `fatima.noor@example.com`
- `omar.s@example.com`
- `ayesha.t@example.com`
- `bilal.ahmed@example.com`

**Login URL:** `/login` (or **Login** in the header / footer)
