/**
 * Database seeding is disabled while the app runs on local dummy data.
 * After the Nest.js backend is ready, replace this script with one that calls your API or DB.
 */
console.log("No seed action: the app uses in-memory demo data in src/data/localStore.ts.");
